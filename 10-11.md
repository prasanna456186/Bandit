# Level 10 → Level 11

`data.txt` is base64 this time, level says so directly.

## What I did

```bash
base64 -d data.txt
```

`-d` decodes instead of encoding. That printed the password straight to the terminal.

## Password for bandit11

`pYfOY6HwUsDj5rL9UvyhU7MCmv8vN5Ro`
